// `CollapsibleSection` lives in components.slint next to `ToggleSwitch`, which it
// uses -- defining it here too would need a circular import. This file only
// keeps the older `@origami/collapsible_section.slint` import path working.
export { CollapsibleSection } from "components.slint";
